# /mailing-list page images

Upload these here (GitHub: Add file -> Upload files). Exact names + `.jpg`:

| File | What it is | Notes |
|---|---|---|
| `football-schedule.jpg` | The Baltimore football schedule magnet | The tall magnet artwork. Straight-on. ~1000px wide, < 400 KB. |
| `baseball-schedule.jpg` | The Baltimore baseball schedule magnet | Same. |
| `am-card.jpg` | The "This Card Makes You 3% Smarter" card | The open/front of the card, landscape. ~1400px wide, < 500 KB. |
| `hero.jpg` (optional) | Wide background photo for the top of the page | Envelopes / desk / mailing scene. ~1800px wide, < 500 KB. If not added, the hero just uses a dark background - still looks fine. |

Until a file is here the page shows a clean "image coming soon" placeholder in
that spot (the hero shows nothing, just the dark background).

Compress first (tinypng.com). Do NOT upload raw phone photos (10+ MB).
The page reads these paths directly - no build step, no config.
